# Plots for Consensus Clustering

Raise an error for now. Suggest users to use `output_file` parameter in
[`gly_consensus_clustering()`](https://glycoverse.github.io/glystats/reference/gly_consensus_clustering.html)
instead.

## Usage

``` r
# S3 method for class 'glystats_cc_res'
autoplot(object, ...)
```

## Arguments

- object:

  A `glystats_cc_res` object.

- ...:

  Ignored.

## Value

A ggplot object.
